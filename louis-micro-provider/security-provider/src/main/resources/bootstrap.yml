louis:
  token:
    issuer: http:/github.louis.com
    refresh-exp-time: 30
    signing-key: battcn
    expiration-time: 30
  security:
    oauth2:
      clients:
        clientId: admin
        clientSecret: admin

#配置配置中心
spring:
  cloud:
    config:
      profile: dev
      uri: http://localhost:8871
