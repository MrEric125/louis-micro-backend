
eureka:
  instance:
    prefer-ip-address: true
    appname: comment-service-web
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  profiles:
    active: dev

